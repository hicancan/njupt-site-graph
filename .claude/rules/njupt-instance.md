# NJUPT instance rules

- Treat `configs/sites/<site_id>/` as site modeling source of truth.
- Treat `data/sites/<site_id>/index/` as generated structured package.
- Treat `docs/research/` as human-readable audit notes.
- Backfeed reusable model, schema, and crawler improvements to `D:/code/github/hicancan/static-site-graph`.
- Do not modify `D:/code/github/hicancan/njupt-search` unless the explicit task is integration/export.
